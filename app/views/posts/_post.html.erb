<%= link_to(user_post_path(@user, post)) do%>
  <div class="comment-card border-deco all-user-post">
    <div class="comment-info">
      <h3>Post <%= post_counter + 1 %></h3>
      <p><%= post.text %></p>
    </div>
    <div class="counter-card d-flex">
      <span>Total Comments: <%= post.comments_counter%></span>
      <span>Total Likes: <%= post.likes_counter%></span>
    </div>
  </div>
<% end %>